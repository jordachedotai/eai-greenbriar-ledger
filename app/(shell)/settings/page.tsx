export default function SettingsPage() {
  return <div className="px-7 py-6 text-[15px] text-mut">Company names and the deal lead come from data/source/companies.json and data/users.json. Nothing to set here yet.</div>;
}
